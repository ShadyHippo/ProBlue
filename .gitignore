# Raw measurement logs.
logs/*

# Vendored third-party reference material (RE notes, nxbt). Read-only, local
# only — not redistributed with this project.
external_docs/

# Pristine upstream trees, materialized by tools/fetch-pristine.zsh for
# diffing/rebuilding. Regenerable from the pinned nixpkgs; the pin is recorded
# in src/MANIFEST.md. The reviewed artifacts under src/ ARE tracked.
build/
